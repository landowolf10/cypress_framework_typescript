let sideMenu: string = '.oxd-sidepanel-body';
let title: string = '.oxd-topbar-header-breadcrumb > .oxd-text';
let userProfile: string = '.oxd-userdropdown-tab';

export {
    sideMenu,
    title,
    userProfile
}